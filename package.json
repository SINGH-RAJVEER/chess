{
	"name": "chess-monorepo",
	"private": true,
	"packageManager": "bun@1.2.22",
	"workspaces": [
		"apps/api",
		"apps/web",
		"apps/engine",
		"packages/types",
		"packages/db"
	],
	"scripts": {
		"lockfile:generate": "bun install --lockfile-only",
		"dev": "nx run-many -t dev -p api engine web --parallel=3",
		"build": "nx run-many -t build --all",
		"test": "nx run-many -t test --all",
		"lint": "nx run-many -t lint --all",
		"format": "nx run-many -t format --all",
		"check": "nx run-many -t check --all",
		"typecheck": "nx run-many -t typecheck --all",
		"affected:build": "nx affected -t build",
		"affected:lint": "nx affected -t lint",
		"affected:test": "nx affected -t test",
		"db:generate": "nx run db:db:generate",
		"db:migrate": "nx run db:db:migrate",
		"db:studio": "nx run db:db:studio",
		"clean": "nx run-many -t clean --all"
	},
	"devDependencies": {
		"@biomejs/biome": "^2.4.7",
		"nx": "^23.0.0"
	}
}
