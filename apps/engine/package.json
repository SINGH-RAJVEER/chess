{
  "name": "@chess/engine",
  "private": true,
  "scripts": {
    "dev": "cargo run",
    "build": "cargo build --release",
    "test": "cargo test",
    "lint": "cargo clippy --all-targets --all-features -- -D warnings",
    "format": "cargo fmt --all",
    "check": "cargo check",
    "clean": "cargo clean"
  }
}
